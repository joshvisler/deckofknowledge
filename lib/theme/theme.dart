import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData get light {
    return ThemeData(colorSchemeSeed: Colors.orange, useMaterial3: true);
  }

  static ThemeData get dark {
    return ThemeData(colorSchemeSeed: Colors.orange, useMaterial3: true);
  }
}
